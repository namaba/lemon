<div class="panel panel-default">
  <div class="table-responsive">
    <table class="table table-hover">
      <thead>
        <tr>
          <th>ID</th>
          <th>写真</th>
          <th>名前</th>
          <th>pass</th>
          <th></th>
          <th></th>
          <th>いいね数</th>
          <th>ありがとう数</th>
        </tr>
      </thead>
      <tbody class="text-left">
        <% @users.each do |user| %>
        <tr>
          <td><%= link_to "#{user.id}", "/users/#{user.id}" %></td>
          <th><%= image_tag user.image1, :size => '50' %></th>
          <td><%= user.user.name %>
            <p><%= user.user.email%></p>
          </td>
          <td>lemon1234</td>
          <td><%= link_to "チャット", premium_messages_path(user_id: user.id), class: "btn btn-success" %></td>
          <td><%= link_to "つぶやき", "", class: "btn btn-warning" %></td>
          <td></td>
          <td></td>
        </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>

